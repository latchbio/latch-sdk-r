mention that u can see project and table ids in the sidebar
document internal APIs properly
make a test account for Registry demo
add more examples
examples are if'd-out right now
add registry mutations
